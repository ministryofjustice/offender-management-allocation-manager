<div class="govuk-grid-row">
  <% poms.each do |pom| %>
    <div class="govuk-grid-column-one-quarter pom-data <%= current_pom_id == pom.staff_id ? 'current-pom' : '' %>">
      <%= yield pom %>
    </div>
  <% end %>
</div>
