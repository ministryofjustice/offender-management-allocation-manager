<li class="moj-primary-navigation__item">
  <% if current_page?(path) || controllers.any? { |name, actions| name == controller_name.to_sym && (actions.empty? || actions.include?(action_name.to_sym)) } %>
    <%= link_to text, path, class: "moj-primary-navigation__link", aria: { current: "page" } %>
  <% else %>
    <%= link_to text, path, class: "moj-primary-navigation__link" %>
  <% end %>
</li>
