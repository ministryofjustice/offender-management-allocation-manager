<% current_help_step = HelpStep.find(0) %>
<%= render :partial => "content", locals: { current_help_step: current_help_step } %>
<h1 class="part-title"><%= current_help_step.heading %></h1>

<div class="govuk-grid-column">
  <div class="govuk-body">
    <p>Follow the steps in this guide to add staff members to the POM caseload service and start making allocations.</p>

    <p>
      These tasks must be completed by a head of offender management delivery (HOMD), head of offender management
      services (HOMS), case administrator or local system administrator.
    </p>

    <%=
      render(
        :partial => "next_link",
        locals: { url: help_step1_path, next_page_title: 'List new staff members’ details'}
      )
    %>
  </div>
</div>
