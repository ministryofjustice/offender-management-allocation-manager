<%= render :partial => "case_info_content", locals: { page: 0 } %>

<div class="govuk-grid-column">
  <div class="govuk-body">
    <h1 class="part-title">Overview</h1>
    <p>The allocations service uses information stored in both NOMIS and nDelius. Occasionally there are conflicts in matching prisoner’s records, or key information is missing.</p>

    <p>It’s important that the information is kept up to date in both NOMIS and nDelius so that we make correct case matches and calculate accurate handover dates.</p>
  </div>

  <%= render :partial => "next_link", locals: { url: updating_ndelius_path, next_page_title: 'Updating nDelius'} %>
</div>
