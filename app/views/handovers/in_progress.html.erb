<%=
  if @pom_view
    description = 'Discuss information about the case with the COM,' \
                  'highlighting any pre-release or pre-parole needs you have identified.'
  else
    description = 'The POM should start sharing information about the case with the COM, ' \
                   'highlighting any pre-release or pre-parole needs they have identified.'
  end

  render 'shared_in_progress',
         heading: 'Handovers in progress',
         description: description,
         table_class: 'in-progress-handovers',
         no_cases_message: 'No handovers in progress right now'
%>
