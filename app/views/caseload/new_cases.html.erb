<% content_for :title, "New cases – Digital Prison Services" %>

<% content_for :switcher do %>
  <%= render '/layouts/prison_switcher' %>
<% end %>

<% if @pom.blank? %>
  <h2 class="govuk-heading-l">No new cases </h2>
<% else %>
  <h2 class="govuk-heading-l">New cases</h2>

  <% if @prison.womens? %>
    <%= render 'caseload_table', header_partial: 'female_headers', collection_partial: 'female', collection: @new_cases %>
  <% else %>
    <%= render 'caseload_table', header_partial: 'male_headers', collection_partial: 'male', collection: @new_cases %>
  <% end %>
<% end  %>
