{{- if .Values.cronjobs.integration_tests_cleanup.enabled }}
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: integration-tests-cleanup
spec:
  schedule: {{ .Values.cronjobs.integration_tests_cleanup.schedule }}
  concurrencyPolicy: Replace
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      ttlSecondsAfterFinished: 120
      template:
        spec:
          serviceAccountName: {{ index .Values "generic-service" "serviceAccountName" }}
          restartPolicy: OnFailure
          securityContext:
            {{- with index .Values "generic-service" }}{{ toYaml .podSecurityContext | nindent 12 }}{{ end }}
          containers:
          - name: integration-tests-cleanup
            image: {{ with index .Values "generic-service" }}{{ .image.repository }}:{{ .image.tag }}{{ end }}
            imagePullPolicy: Always
            securityContext:
              {{- with index .Values "generic-service" }}{{ toYaml .securityContext | nindent 14 }}{{ end }}
            command: ["sh", "-c", "bundle exec rake integration_tests:clean_up"]
              {{- include "moic-helpers.envs_without_prometheus_metrics" . | nindent 12 }}
            resources:
              limits:
                memory: 1000Mi
                cpu: 1000m
              requests:
                memory: 500Mi
                cpu: 50m
---
{{- end }}
