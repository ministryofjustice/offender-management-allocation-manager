apiVersion: v1
kind: Service
metadata:
  name: allocation-manager
  namespace: offender-management-production
spec:
  ports:
  - protocol: TCP
    port: 3000
    targetPort: 3000
  selector:
    app: allocation-manager
